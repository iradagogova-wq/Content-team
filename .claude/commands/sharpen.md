---
description: Прогнать готовый текст через Психолога — найти, где он не работает, и починить
argument-hint: [файл сценария, ссылка или текст прямо в сообщении]
---

Что править: **$ARGUMENTS**

_(пусто — возьми самый свежий файл из `pipeline/scripts/` и скажи, какой взял)_

Зови `psych-editor` **настоящим вызовом субагента**. Сам не разбирай:
он читает `brand/psychology.md`, ты — нет.

Работает с чем угодно: сценарий из `pipeline/scripts/`, старый текст, черновик
прямо в сообщении, чужой материал для разбора.

## Что вернуть мне

1. Таблицу блоков — где пусто.
2. Четыре ответа: зачем досматривать, сохранять, пересылать, что сделать после.
3. Три правки: было → стало.
4. Вердикт.

Не пересказывай разбор своими словами и не добавляй от себя четвёртую правку.

## Если правки приняты

Внеси их в файл сценария и скажи, что изменилось. Файл не переписывай целиком —
меняются три строки, остальное остаётся как было.
